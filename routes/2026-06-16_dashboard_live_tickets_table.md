# 2026-06-16 Dashboard Live Tickets Table

Converted the dashboard's live tickets area from card tiles to a table layout so ticket rows read consistently across the app.

## Notes
- Preserved the existing quick status actions and overflow menu behavior.
- Kept the shared floating menu portal fix in place so menus still render above cards and panels.
- Updated the dashboard test selectors to target table rows instead of card articles.
